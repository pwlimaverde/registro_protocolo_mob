name: decoder_upload_csv_web_package
description: Package responsavel pela decodificação dos arquivos csv em string
publish_to: 'none'
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=3.3.1"

dependencies:
  flutter:
    sdk: flutter
  dependencies_module:
    path: ../../modulos/dependencies_module

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: